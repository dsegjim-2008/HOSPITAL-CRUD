# Copia este archivo como .env y ajusta los valores a tu entorno
# El .env real está ignorado por git (.gitignore)

# URL base de la API del backend Spring Boot
VITE_API_URL=http://localhost:8081/api
