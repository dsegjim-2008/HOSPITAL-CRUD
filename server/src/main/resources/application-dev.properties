# Configuración exclusiva del entorno de desarrollo
# Este archivo solo se carga cuando SPRING_PROFILES_ACTIVE=dev
# No incluir en builds de producción

# Muestra las queries SQL generadas por Hibernate en la consola
spring.jpa.show-sql=true
